To install the package, run the following command line expression:
```
pip install .
```

To install the package in development mode / editable mode, run the following command line expression:
```
pip install -e .
```

To install the package in development mode /editable mode with additional "dev" dependencies, run the following command line expression:
```
pip install -e ".[dev]"
```